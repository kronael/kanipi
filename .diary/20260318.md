# 2026-03-18

## v1.10.1 — approve override + deploy

Shipped v1.10.1. Changes since v1.10.0:

- **approve override**: `/approve` now allows re-approving already-approved
  entries (re-approval sets new world if needed, re-registers route). World
  folder already existing on fs is no longer an error — copies prototype only
  if folder absent, then routes to existing folder either way.
- **Tests updated**: 19 tests for approve.test.ts, 19 for onboarding.test.ts.

## Deploy notes

- Deployed kanipi:v1.10.1 → kanipi-marinade:latest, kanipi-krons:latest, kanipi-rhias:latest
- Restarted marinade and rhias
- kanipi_krons: still failed (WhatsApp 401 logout from 2026-03-17, needs `/setup` re-auth)
- kanipi_rhias: was crash-looping (WA creds invalid); now stable with backoff retry
  thanks to v1.10.0 scheduleReconnect fix. Needs re-pairing to recover fully.

## Disk cleanup

Disk was at 99% (23MB free), causing atlas agent container to fail with ENOSPC.
Pruned dangling images + deleted old version tags (v1.0–v1.6). Now at 76% (23GB free).
