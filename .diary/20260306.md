# 2026-03-06

## Docs overhaul — krons.fiu.wtf/kanipi

Session `c82ee900-fcae-4041-859c-c04705af0165`.

Rewrote the main kanipi docs page at `/srv/data/takopipi_krons/web/kanipi/index.html`
to match v1 reality. The old page was significantly outdated — claimed v0.1.2,
18 files/4856 LOC, 3 channels, config.yaml, voice "coming", no email.

### Key fixes

- Version: v0.1.2 → v0.6.3
- Stats: 38 source files / 8,538 LOC; 32 test files / 9,671 LOC
- Channels: 3 → 5 (added Email, Web/Slink)
- Config: "config.yaml" → ".env + env vars"
- Voice transcription + file sending: marked as shipped
- Email: moved from "V2 planned" to V1 shipped
- Removed Wire/Tap routing concepts (never built) — replaced with actual
  architecture: group routing, action registry, MCP sidecars
- Removed false Arctic OAuth claim (Slink uses HMAC JWT)
- Fixed SSE contradiction (shipped in V1 but also listed as V2 planned)

### New V1 features documented

Action registry, MCP sidecars, diary memory, circuit breaker,
hot-patching (bunx tsc at runtime), file sending, delegation.

### V2 section rewritten

Now reflects actual v2 specs: topics, memory layers (work/episodes/facts),
agent personas, workflow pipelines, IPC→MCP migration. Removed Wire/Tap.

### Critique round fixes

- Product count 4→3 (removed Takopipi from product cards, it's the host not a product)
- Evangelist card: present→future tense for unbuilt middleware
- "Hierarchical routing" → "group routing + delegation" with accurate description

### Also this session (before context compaction)

- Fixed TS2353 compile error in agent-runner crashing all containers
- Removed retry loops on agent errors — circuit breaker pattern instead
- Added agent-runner to build/lint/pre-commit (compile errors caught before deploy)
- Switched npx→bunx throughout (Makefile, pre-commit, Dockerfile)
- Agent container hot-patching: bunx tsc at runtime, validate-only build in Dockerfile
- Created v2/work.md spec (current task state, inspired by brainpro WORKING.md)
- Updated diary spec: 2 most recent files with relative time ("ago")
- Researched brainpro memory system, extended v2 specs

## Diary spec + v1 audit + v0.6.3 release

Session `c82ee900-fcae-4041-859c-c04705af0165` (continued after compaction).

### V1 spec audit

Audited all 22 critique items — 14 already fixed, rest resolved this session.
JID format unified across all specs: `telegram:`, `whatsapp:`, `discord:`,
`email:`, `web:`. Deleted `specs/1/critique.md`.

### Diary spec finalized

`specs/1/L-memory-diary.md` + `container/skills/diary/SKILL.md`:

- Progressive writing via file tools (survives crashes, no transcript dump)
- Three triggers: `/diary` skill, PreCompact hook, Stop hook at 100 turns
- Stop hook: `stop_hook_active` guard prevents loops; nudge text from skill
  `description` frontmatter (single source of truth)
- Summary: terse YAML — project context + up to 5 clearly important tasks
- Tasks tracked through entries ("New task — auth: ...", "deploy: done")
- Gateway injects 2 most recent summaries with relative time on session reset
- Deletes old transcript dump code (`createPreCompactHook`, `conversations/`)

### Media-aware file sending

Telegram: extension-based routing to sendPhoto/sendVideo/sendAnimation/sendAudio.
WhatsApp: MIME-based routing to image/video/audio message formats.

### Refine pass

No bugs found. Removed dead `method` var in telegram.ts. Confirmed APIs correct.

### Released v0.6.3, deployed to sloth

`package.json` bumped, both images built and tagged, `systemctl restart kanipi_sloth`.

## Migrated Marinade Atlas from ElizaOS to kanipi

Switched `eliza_atlas_marinade` (ElizaOS monolith) → `kanipi_marinade`
(containerized Claude agents). Same bot `@mnde_atlas_bot`, same Telegram chat.

- Created instance via `kanipi create marinade`
- Copied 50+ facts files verbatim from eliza data dir
- Symlinked 8GB codebase repos (not duplicated)
- Wrote CLAUDE.md with Marinade Atlas personality + honesty rules
- character.json with bio/topics/style from original
- Registered group `telegram:-5174030672` → main
- Stopped/disabled `eliza_atlas_marinade`, started `kanipi_marinade` on port 49180
- Spec documented in `specs/atlas/setup.md`

Agent gets facts at `/workspace/group/facts/` and codebase at
`/workspace/group/refs/codebase/` with full Claude Code SDK for search.
Bare but functional — deep research subagents and git refresh are future.
